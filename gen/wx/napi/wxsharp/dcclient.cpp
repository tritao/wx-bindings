// ----------------------------------------------------------------------------
// <auto-generated>
// This is autogenerated code by CppSharp.
// Do not edit this file or all your changes will be lost after re-generation.
// </auto-generated>
// ----------------------------------------------------------------------------
#include <wxsharp/dcclient.h>
#include <node/node_api.h>
#include <assert.h>
#include <stdio.h>
#include "NAPIHelpers.h"

extern napi_ref ctor_wxSharp_GraphicsContext_GraphicsContext;
extern napi_ref ctor_wxSharp_GraphicsPath_GraphicsPath;
extern napi_ref ctor_wxSharp_GraphicsMatrix_GraphicsMatrix;
extern napi_ref ctor_wxSharp_GraphicsPen_GraphicsPen;
extern napi_ref ctor_wxSharp_GraphicsBrush_GraphicsBrush;
extern napi_ref ctor_wxSharp_GraphicsFont_GraphicsFont;
extern napi_ref ctor_wxSharp_GraphicsBitmap_GraphicsBitmap;
extern napi_ref ctor_wxSharp_Event_Event;
extern napi_ref ctor_wxSharp_Point_Point;
extern napi_ref ctor_wxSharp_Rect_Rect;
extern napi_ref ctor_wxSharp_Color_Color;
extern napi_ref ctor_wxSharp_SizerItem_SizerItem;
extern napi_ref ctor_wxSharp_Size_Size;
extern napi_ref ctor_wxSharp_Window_Window;
extern napi_ref ctor_wxSharp_EvtHandler_EvtHandler;
extern napi_ref ctor_wxSharp_VisualAttributes_VisualAttributes;

static void dtor_wxSharp_WindowDC_WindowDC(napi_env env, void* finalize_data, void* finalize_hint)
{
}

static napi_ref ctor_wxSharp_WindowDC_WindowDC;

// wxSharp::WindowDC::WindowDC
static napi_value callback_method_wxSharp_WindowDC_WindowDC(napi_env env, napi_callback_info info)
{
    napi_status status;
    napi_value _this;
    size_t argc;
    status = napi_get_cb_info(env, info, &argc, nullptr, &_this, nullptr);
    assert(status == napi_ok);

    if (argc < 1 || argc > 1)
    {
        status = napi_throw_type_error(env, nullptr, "Unsupported number of arguments");
        assert(status == napi_ok);

        return nullptr;
    }

    napi_value args[argc];
    status = napi_get_cb_info(env, info, &argc, args, nullptr, nullptr);
    assert(status == napi_ok);

    napi_valuetype types[argc];
    for (size_t i = 0; i < argc; i++)
    {
        status = napi_typeof(env, args[i], &types[i]);
        assert(status == napi_ok);
    }

    wxSharp::WindowDC* instance = nullptr;

    if (NAPI_IS_OBJECT(types[0]))
        goto overload0;

    if (NAPI_IS_OBJECT(types[0]))
        goto overload1;

    goto error;

error:
    status = napi_throw_type_error(env, nullptr, "Unsupported argument type");
    assert(status == napi_ok);

    return nullptr;

    // WindowDC(::wxSharp::Window* win)
overload0:
    {
        wxSharp::Window* win_instance;
        status = napi_unwrap(env, _this, (void**) &win_instance);
        auto __arg0 = win_instance;

        instance = new wxSharp::WindowDC(__arg0);
    }

    // WindowDC
overload1:
    {
        wxSharp::WindowDC* _0_instance;
        status = napi_unwrap(env, _this, (void**) &_0_instance);
        auto &__arg0 = *_0_instance;

        instance = new wxSharp::WindowDC(__arg0);
    }
    napi_ref result;
    status = napi_wrap(env, _this, instance, dtor_wxSharp_WindowDC_WindowDC, nullptr, &result);
    assert(status == napi_ok);

    printf("wxSharp::WindowDC::WindowDC: %lu\n", argc);
    return _this;
}

static napi_value register_class_wxSharp_WindowDC(napi_env env)
{
    napi_status status;
    napi_property_attributes attributes = (napi_property_attributes) (napi_default | napi_enumerable);
    napi_property_descriptor props[] =
    {
        // { utf8name, name, method, getter, setter, value, attributes, data }

    };

    napi_value constructor;
    status = napi_define_class(env, "WindowDC", NAPI_AUTO_LENGTH, callback_method_wxSharp_WindowDC_WindowDC, nullptr, sizeof(props) / sizeof(props[0]), props, &constructor);
    assert(status == napi_ok);

    status = napi_create_reference(env, constructor, 1, &ctor_wxSharp_WindowDC_WindowDC);
    assert(status == napi_ok);

    return constructor;
}

static void dtor_wxSharp_ClientDC_ClientDC(napi_env env, void* finalize_data, void* finalize_hint)
{
}

static napi_ref ctor_wxSharp_ClientDC_ClientDC;

// wxSharp::ClientDC::ClientDC
static napi_value callback_method_wxSharp_ClientDC_ClientDC(napi_env env, napi_callback_info info)
{
    napi_status status;
    napi_value _this;
    size_t argc;
    status = napi_get_cb_info(env, info, &argc, nullptr, &_this, nullptr);
    assert(status == napi_ok);

    if (argc < 1 || argc > 1)
    {
        status = napi_throw_type_error(env, nullptr, "Unsupported number of arguments");
        assert(status == napi_ok);

        return nullptr;
    }

    napi_value args[argc];
    status = napi_get_cb_info(env, info, &argc, args, nullptr, nullptr);
    assert(status == napi_ok);

    napi_valuetype types[argc];
    for (size_t i = 0; i < argc; i++)
    {
        status = napi_typeof(env, args[i], &types[i]);
        assert(status == napi_ok);
    }

    wxSharp::ClientDC* instance = nullptr;

    if (NAPI_IS_OBJECT(types[0]))
        goto overload0;

    if (NAPI_IS_OBJECT(types[0]))
        goto overload1;

    goto error;

error:
    status = napi_throw_type_error(env, nullptr, "Unsupported argument type");
    assert(status == napi_ok);

    return nullptr;

    // ClientDC(::wxSharp::Window* win)
overload0:
    {
        wxSharp::Window* win_instance;
        status = napi_unwrap(env, _this, (void**) &win_instance);
        auto __arg0 = win_instance;

        instance = new wxSharp::ClientDC(__arg0);
    }

    // ClientDC
overload1:
    {
        wxSharp::ClientDC* _0_instance;
        status = napi_unwrap(env, _this, (void**) &_0_instance);
        auto &__arg0 = *_0_instance;

        instance = new wxSharp::ClientDC(__arg0);
    }
    napi_ref result;
    status = napi_wrap(env, _this, instance, dtor_wxSharp_ClientDC_ClientDC, nullptr, &result);
    assert(status == napi_ok);

    printf("wxSharp::ClientDC::ClientDC: %lu\n", argc);
    return _this;
}

static napi_value register_class_wxSharp_ClientDC(napi_env env)
{
    napi_status status;
    napi_property_attributes attributes = (napi_property_attributes) (napi_default | napi_enumerable);
    napi_property_descriptor props[] =
    {
        // { utf8name, name, method, getter, setter, value, attributes, data }

    };

    napi_value constructor;
    status = napi_define_class(env, "ClientDC", NAPI_AUTO_LENGTH, callback_method_wxSharp_ClientDC_ClientDC, nullptr, sizeof(props) / sizeof(props[0]), props, &constructor);
    assert(status == napi_ok);

    status = napi_create_reference(env, constructor, 1, &ctor_wxSharp_ClientDC_ClientDC);
    assert(status == napi_ok);

    return constructor;
}

static void dtor_wxSharp_PaintDC_PaintDC(napi_env env, void* finalize_data, void* finalize_hint)
{
}

static napi_ref ctor_wxSharp_PaintDC_PaintDC;

// wxSharp::PaintDC::PaintDC
static napi_value callback_method_wxSharp_PaintDC_PaintDC(napi_env env, napi_callback_info info)
{
    napi_status status;
    napi_value _this;
    size_t argc;
    status = napi_get_cb_info(env, info, &argc, nullptr, &_this, nullptr);
    assert(status == napi_ok);

    if (argc < 1 || argc > 1)
    {
        status = napi_throw_type_error(env, nullptr, "Unsupported number of arguments");
        assert(status == napi_ok);

        return nullptr;
    }

    napi_value args[argc];
    status = napi_get_cb_info(env, info, &argc, args, nullptr, nullptr);
    assert(status == napi_ok);

    napi_valuetype types[argc];
    for (size_t i = 0; i < argc; i++)
    {
        status = napi_typeof(env, args[i], &types[i]);
        assert(status == napi_ok);
    }

    wxSharp::PaintDC* instance = nullptr;

    if (NAPI_IS_OBJECT(types[0]))
        goto overload0;

    if (NAPI_IS_OBJECT(types[0]))
        goto overload1;

    goto error;

error:
    status = napi_throw_type_error(env, nullptr, "Unsupported argument type");
    assert(status == napi_ok);

    return nullptr;

    // PaintDC(::wxSharp::Window* win)
overload0:
    {
        wxSharp::Window* win_instance;
        status = napi_unwrap(env, _this, (void**) &win_instance);
        auto __arg0 = win_instance;

        instance = new wxSharp::PaintDC(__arg0);
    }

    // PaintDC
overload1:
    {
        wxSharp::PaintDC* _0_instance;
        status = napi_unwrap(env, _this, (void**) &_0_instance);
        auto &__arg0 = *_0_instance;

        instance = new wxSharp::PaintDC(__arg0);
    }
    napi_ref result;
    status = napi_wrap(env, _this, instance, dtor_wxSharp_PaintDC_PaintDC, nullptr, &result);
    assert(status == napi_ok);

    printf("wxSharp::PaintDC::PaintDC: %lu\n", argc);
    return _this;
}

static napi_value register_class_wxSharp_PaintDC(napi_env env)
{
    napi_status status;
    napi_property_attributes attributes = (napi_property_attributes) (napi_default | napi_enumerable);
    napi_property_descriptor props[] =
    {
        // { utf8name, name, method, getter, setter, value, attributes, data }

    };

    napi_value constructor;
    status = napi_define_class(env, "PaintDC", NAPI_AUTO_LENGTH, callback_method_wxSharp_PaintDC_PaintDC, nullptr, sizeof(props) / sizeof(props[0]), props, &constructor);
    assert(status == napi_ok);

    status = napi_create_reference(env, constructor, 1, &ctor_wxSharp_PaintDC_PaintDC);
    assert(status == napi_ok);

    return constructor;
}

void register_wxsharp_dcclient(napi_env env, napi_value exports)
{
    napi_value value;

    value = register_class_wxSharp_WindowDC(env);
    NAPI_CALL_NORET(env, napi_set_named_property(env, exports, "WindowDC", value));

    value = register_class_wxSharp_ClientDC(env);
    NAPI_CALL_NORET(env, napi_set_named_property(env, exports, "ClientDC", value));

    value = register_class_wxSharp_PaintDC(env);
    NAPI_CALL_NORET(env, napi_set_named_property(env, exports, "PaintDC", value));
}
